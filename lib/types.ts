import type { JSX } from "react";

export interface Service {
  icon: JSX.Element;
  title: string;
  desc: string;
}

export interface Reason {
  icon: JSX.Element;
  title: string;
  desc: string;
}